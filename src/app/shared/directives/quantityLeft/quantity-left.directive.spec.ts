import { QuantityLeftDirective } from './quantity-left.directive';

describe('QuantityLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityLeftDirective();
    expect(directive).toBeTruthy();
  });
});
