import { ExpiredDateDirective } from './expired-date.directive';

describe('ExpiredDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpiredDateDirective();
    expect(directive).toBeTruthy();
  });
});
